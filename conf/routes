# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
POST /                           controllers.Application.registerProfile
POST /addNewMatch                          controllers.Application.addNewMatch


GET     /signup                           controllers.Signup.index
POST /signup                           controllers.Signup.registerProfile


## POPUPS using jquery dialog2
GET     /createNewLeague                           controllers.Application.createNewLeague
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
